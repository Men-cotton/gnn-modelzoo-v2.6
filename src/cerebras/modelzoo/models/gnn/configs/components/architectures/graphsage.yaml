extends: input_pipelines/neighbor.yaml

trainer:
  init:
    model:
      architecture:
        type: graphsage
        hidden_dim: 1024
        num_layers: 3
        dropout: 0.5
        aggregator: mean
      task:
        to_float16: True
